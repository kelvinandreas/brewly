run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gofmt
    - goimports
    - revive

linters-settings:
  goimports:
    local-prefixes: github.com/your-handle/brewly

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
